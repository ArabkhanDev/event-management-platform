# OS
.DS_Store
Thumbs.db

# Claude Code — machine-local permission settings, not meant to be shared
.claude/settings.local.json

# Backend (Gradle/Java)
backend/.gradle/
backend/build/
backend/.idea/
backend/out/
*.iml

# Frontend (node_modules, dist, etc. — see frontend/.gitignore for the rest)
frontend/.idea/

# IDE
.idea/
.vscode/*
!.vscode/extensions.json

# Local env files — never commit real secrets, only *.env.example
.env
.env.local
frontend/.env
!frontend/.env.example
